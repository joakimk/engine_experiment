source "http://rubygems.org"

gemspec

# jquery-rails is used by the dummy application
gem "jquery-rails"
gem "haml-rails"

group :assets do
  gem 'sass-rails', "~> 3.2.3"
  gem 'coffee-rails', "~> 3.2.1"
  gem 'uglifier', '>= 1.0.3'
end

eval(File.read(File.join(File.dirname(__FILE__), 'common_gems.rb')))
